{
  "configurations": [
  {
    "type": "lldb",
    "request": "launch",
    "name": "Launch",
    "program": "${workspaceFolder}/calcoloAree.exe",
    "args": [],
    "cwd": "${workspaceFolder}"
  }
  ]
}